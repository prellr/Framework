# Resolution-source feature-cut guard deployment receipt

- Product version: `0.1.0`
- Deployed source commit: `95729a8`
- Deployed at: `2026-07-25T01:05:04Z`
- Server: Server2 (`/Users/admin/jester-analytics`)
- Recovery point: `/Users/admin/jester-releases/20260725T010049Z`
- Source archive SHA-256: `02e62708a34e9ee019bc26e2e1b6cc8f94c30c9e849c8d0f7fe4a2f2bbe66875`
- Database dump SHA-256: `7e49a1fd03fccd1be717234c116eeb8e27ef5ea7652cda962db602cf849fd913`
- API image: `sha256:2698cb78c0d35a0f99f7eac4e54e283269be59c418943258191743429e1fa647`
- Worker image: `sha256:7e40f038ab0821f70c2cc0327d7860671855b9331d305b4a504ea9a140a49974`
- Nginx image: `sha256:8be258eaaffcc329d821476d0d7059f536e9124a7d356a6b03e9587ded552a5d`

## Research disposition

The frozen 57-member strategy-by-timeframe family remains unchanged. The next independent lane is
the already-preregistered resolution-source basis convergence hypothesis, which compares the
Chainlink settlement reference with the causal Hyperliquid public midpoint. Its feature values
remain sealed until every pair passes the inherited 100,000-row, three-day, 500-block floor.

This deployment adds the missing preprocessing gate required by Formula Lab:

- a six-pair, horizon-neutral reference contract;
- exact p05/p25/p50/p75/p95, count, and IQR retention for the six preregistered basis, change,
  persistence, and source-age metrics;
- canonical JSON serialization and SHA-256 verification;
- rejection of pooled, incomplete, duplicate, degenerate, stale, invalid, or tampered inputs;
- an immutable first-write artifact;
- a minimum 30-minute delay and subsequent 15-minute grid boundary before any later experiment or
  paper identity may start; and
- a fail-closed freeze script whose feature query remains unreachable before the inherited tape
  readiness predicate passes.

The artifact selects no direction, horizon, threshold, market, side, formula, strategy, paper
decision, or result.

## Pre-deployment gates

- 425 API service tests passed.
- API and web TypeScript checks passed.
- The web production build passed.
- `git diff --check` passed.
- The Compose configuration validated.
- A checksummed source archive and gzip-validated database dump were created before sync.
- The source sync contained only the seven intended files and performed no deletes.

## Post-deployment verification

- API health returned `ok`.
- API, worker, nginx, Postgres, and Redis were running; health-enabled services were healthy.
- Host load was `1.50 / 1.81 / 1.77`.
- The Knowledge article
  `updown-resolution-source-basis-feature-cut-freeze-plan-v1` was created with its
  `kb.preregistration.record` audit entry.
- Formula Lab rendered `frozen updown-resolution-source-basis-feature-cuts-v1 artifact` as an
  explicit live-data prerequisite.
- Strategy Lab rendered the separate guarded-freeze disclosure while keeping the feature report
  locked.
- No browser console errors were observed.

## Fail-closed live proof

At `2026-07-25T01:06Z`, every pair exceeded the count floors:

- 146,249–163,885 rows per pair;
- 553–554 five-minute blocks per pair; and
- 1.948 of the required three days.

The earliest pair-level span unlock is `2026-07-26T02:22:09Z`. An intentional early invocation
returned `refusing resolution-source feature-cut freeze: 0/6 pair distributions ready`, exited
non-zero, and created no artifact. The preregistered plan is the only new Knowledge record.

## Execution boundary

This deployment adds no collector, subscription, database migration, strategy, paper trade, result
query, Crucible run, authentication/signing/submission capability, order route, allocation, wallet,
or fund-moving path. Any later formula experiment or Polymarket translation requires its own future
registration and the unchanged familywise paper verdict gate.
