# Alchemy v0.1.0 deployment receipt

- Product version: `0.1.0`
- Deployed source commit: `ba57d8d`
- Deployed at: `2026-07-25T00:19:20Z`
- Server: Server2 (`/Users/admin/jester-analytics`)
- Recovery point: `/Users/admin/jester-releases/20260725T001640Z`
- Migration ledger: 40 applied migrations
- API image: `sha256:f07f548db59bab474652eb9a369d66e830db350f8c67779acd20020633259d8f`
- Worker image: `sha256:914fe4267af1ca9ebd0410f8af31d4d715dd488f3a1f4f86c9af80e18d585de2`
- Nginx image: `sha256:9fbbfd93e89c0c984b4d0648938cd4ba63d8e5920682597f8ab5949dafbf86e5`

## Pre-deployment gates

- 418 API service tests passed.
- Six Python research-worker tests passed.
- API, web, database, and research-protocol type checks passed.
- The web production build passed.
- The source sync dry-run reported zero deletes and zero generated/cache artifacts.
- A checksummed source archive and compressed database dump were created before sync.

## Frozen launch receipts

The familywise launch receipt ran after `2026-07-25T00:16:00Z` and passed every check:

- exact 57-member executable roster;
- exact four opposite-side comparator exceptions;
- both 5m and 15m control collection;
- an eligible opportunity for every frozen hypothesis;
- registered buckets and clocks only;
- 340 unique candidate bot × market rows; and
- zero pre-boundary Cobra-night rows.

The shadow-connector receipt also passed every check:

- all six registered pairs at both 5m and 15m;
- 72 of 72 valid telemetry plans; and
- authentication, signing, and submission disabled.

Both receipts were outcome-blind and inspected no chosen side, quote, fill, resolution, grade,
return, residual, rank, or P&L.

## Post-deployment verification

- API health returned `ok`.
- API, worker, nginx, Postgres, and Redis were running; health-enabled services were healthy.
- Host load was `1.50 / 1.74 / 1.72`.
- The outcome-blind CLOB status reported 99.0% operational coverage, 100% paired-book coverage,
  zero transport-missing rows, and fresh market data.
- Formula Lab rendered the clarified column dictionary and retained its no-execution guard.
- The cited `alchemy-formula-lab-research-framework-v2` article rendered in Knowledge.
- Strategy Lab rendered the frozen familywise gate, all six assets, and `no order endpoint`.
- No browser console errors were observed.

## Research disposition

This release authorizes continued paper collection only. It does not admit a Polymarket strategy,
pass a forward verdict, enable authentication/signing/submission, or authorize live execution.
